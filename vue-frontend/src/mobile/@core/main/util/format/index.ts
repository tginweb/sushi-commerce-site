export const normalizePhone = require('./normalizePhone').default
export const normalizeEmail = require('./normalizeEmail').default
export const boolean = require('./boolean').default
export const price = require('./price').default
