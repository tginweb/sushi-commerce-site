import {defineStore} from "pinia";
import {Redirect} from "@/core/types";

export const useRedirect = defineStore("redirect", () => {

    const redirect = (redirect: Redirect) => {

    }

    return {
        redirect
    }
})
